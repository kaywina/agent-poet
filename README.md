# agent-poet

A bounded, human-in-the-loop daily poetry agent.

- Generates at most one poem per day
- Enforces safety checks before review
- Requires explicit human approval to publish
- Publishes to a separate static site (agent-workbench)

This project intentionally avoids full autonomy.
